# Focus Forest: scoped audit and next-step plan

Date: 2026-09-17

## Scope and confidence

This is an interim review, NOT the exhaustive every-file/every-function audit requested. The repository was inventoried and selected sections of the manifest, state utilities, worker, tests, constants, error tracing, preview server and README were inspected. Some earlier reads were truncated. UI source, styles, remaining scripts, historical reports, assets and lifecycle behavior still require systematic inspection. The nested .kilo worktree was not audited or changed; apparent duplication was not verified by hashes. Existing audit documents are not proof of current correctness. No exploit reproduction or market-wide uniqueness investigation was performed.

## Product assessment

Focus Forest 0.3.4 is a local-first Manifest V3 Chromium browsing companion. A mission supplies an intention; navigation relationships form a tree; increasing branch depth invites reflection rather than blocking websites. A garden preserves sessions and compost preserves pages for later. New Tab, popup, settings and dashboard are separate extension pages. The service worker coordinates state and browser events. There are no declared runtime dependencies; Playwright is development-only.

The strongest idea is the combination of intention, explainable navigation history, gentle intervention and permission to keep exploring. Depth measures structural distance, NOT relevance, distraction or productivity. Copy and analytics should preserve that distinction.

This is a promising, coherent prototype. Bounded storage, dependency-light implementation and a non-punitive tone are good foundations. Data correctness, deletion semantics and browser lifecycle reliability should precede additional visual or reward features.

### Is it unique?

Not in individual ingredients: tree-based browsing history, focus companions, save-for-later lists and nature-themed motivation are established categories. The README acknowledges related navigation-tree projects. The combination may be distinctive, but source inspection cannot establish market-wide novelty. Avoid claiming "the first" without a current product comparison. Validate differentiation with users: do they understand why a prompt appeared, feel respected, and return to their own intentions?

## Findings

### Fixed: incorrect calendar duration

C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\background\service-worker.js:554 and :728 defined a day as a maintenance minute multiplied by 24: 24 minutes, not 24 hours. Weekly dates collapsed, streaks counted repeated dates, and the supposed five-year import window was about 30.4 days. Added DAY_MS = 86400000 to C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\shared\constants.js and used it at both sites. Historical damage from past imports is not automatically repaired.

### Fixed after regression: Forget Site skipped storage when only saved items matched

C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\background\service-worker.js:937-948 filtered nodes and compost but returned NO_CHANGE when zero nodes were removed, so a compost-only deletion changed memory without persisting; saved items reappeared after a worker restart. A stored-storage regression in C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\test-service-worker.mjs failed on the original code (forget_saved survived), then passed after counting compost removals toward the changed result. A no-match request still returns null.

Still open on this path: origin metadata and event URLs are not removed, node removal can orphan child relationships, and results are asserted against the mutable cache plus mocked storage rather than a freshly reloaded real storage backend.

### Fixed after regressions: import deduplication, rewards, and merge ordering

C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\background\service-worker.js now merges imports inside the serialized mutation queue, so the merge reads current state after previously queued operations instead of a snapshot taken before queueing. Conflict policies: imported sessions replace matching IDs as whole records and are repositioned newest before the session cap; imported settings replace local settings; compost keeps existing items; rewardHistory unions and deduplicates identical (rewardId, timestamp) occurrences so one catalog reward can still be earned again; onboarding unions. Three regressions in C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\test-service-worker.mjs failed before the fix and passed afterward: repeat imports no longer duplicate sessions and revised records win; local and incoming rewards survive repeated and legacy (rewardHistory-free) imports after a fresh storage read; and an import queued behind a blocked CLEAR_DATA storage write cannot resurrect pre-clear sessions. The fast suite (18 state tests, contract, stress), 13 browser tests, packaging, and syntax/diff checks all passed after the fix. Import still traverses input before output limits apply, and runtime sessions absent from an import are preserved by design.

### Fixed after regressions: compaction retention and change reporting

C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\shared\state.js now retains the first 20 compost items, matching the newest-first insertion and normalization order, and returns whether any persistent change occurred. Two regressions in C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\test-state.mjs failed before the fix and passed afterward: newest-save retention (including a fresh storage read), and compost-only change reporting with a repeated no-op check. All 18 state tests pass. Compaction concurrency and failed-write cache behavior remain outside this fix.

### Fixed after regressions: Forget Site deletion integrity

C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\background\service-worker.js `FORGET_SITE` previously deleted only nodes and compost items, leaving matching origin URLs/titles/tab bindings, matching event records, references to deleted nodes in events, and stale runtime redirects/intervals behind. The new `forgetSite()` also: scrubs matching origin metadata to a neutral extension URL with null tab/window (so the next observation cannot overwrite a surviving node via the placeholder path); deletes matching events and events referencing deleted nodes; detaches children of deleted parents (parentId null, relationship demoted to external/low confidence) while preserving their history; recalculates depths with cycle-bounded traversal and re-derives non-terminal node states; removes stale pendingRedirects/activeIntervals/pendingBranches/activeTabs/navigationHints entries tied to deleted tabs or nodes; and clears the SPA dedupe cache. Two regressions in test-service-worker.mjs failed before the fix (metadata-only deletion, descendant repair with fresh-read persistence) and pass after. Semantics: Forget Site deletes stored records for one host; it does not delete unrelated descendants of forgotten pages, nor change tracking settings.

### Fixed after regressions: compaction failure and cache-safety

C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\shared\state.js `compactStateIfNeeded` mutated the shared cached state object in place before writing. Although its explicit cache assignment already followed a successful write, the earlier in-place mutation exposed unpersisted compaction when the write failed. It now works on a `structuredClone` of the loaded state and adopts the compacted cache only after the storage write succeeds; write failures continue to propagate to callers. One regression in C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\test-state.mjs (failed write leaves cache and durable state intact, verified across a fresh read) failed before the fix and passes after. Full fast suite (including 19 state tests), 13 dashboard browser tests, packaging, syntax, and git diff --check all exit 0 afterward. Alarm-driven compaction now runs inside the worker mutation queue, with one quota check per alarm. A blocked-clear regression in C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\test-service-worker.mjs failed before this change and passes after: maintenance waits for Clear Data before reading state and performs no stale-history write. The latest fast suite passed (6 named worker regressions plus existing behavioral assertions, 19 state tests, contracts and stress); all 13 browser tests and packaging also passed. Installed-extension lifecycle durability remains unverified.

### Security/privacy hardening — not confirmed external exploits


- No storage.setAccessLevel call was found in the searched runtime directories. Restrict history storage to TRUSTED_CONTEXTS after checking callers. Compact message responses do not themselves restrict the extension's own content-script storage API access. Ordinary websites do not thereby gain direct storage access.
- C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\shared\error-tracing.js logs context and stacks; import warnings can include URLs. Redact sensitive components and bound diagnostic strings before retention and logging.
- Import traverses input before output limits apply. Bound input bytes and collections before expensive processing. Bounded stored output does not imply bounded processing work.
- Broad host permissions fit passive tracking but enlarge exposure. Explain permissions clearly and evaluate optional access without silently breaking workflows.
- Fixed 2026-09-18: C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\scripts\preview-trees.mjs now binds to `127.0.0.1` by default, with explicit `HOST` opt-in for remote previews. Its exported startup function allows port-zero tests without import-time listening. Two tests in C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\test-preview.mjs pass against actual listening sockets: default loopback with sample page/renderer/CSP responses, and an explicit host override. These tests now run in `npm test`. This is development-server hardening, not installed-extension lifecycle validation.

Positive controls observed: no web-accessible resources, restrictive script/connect CSP, URL normalization, privileged-operation sender checks, and sender-boundary tests. Static regex contracts are guardrails, not a security certification.

## Performance optimization plan

1. Establish baselines in an installed extension: idle worker wakeups, storage writes/bytes per navigation, message latency, dashboard render time, DOM count, long tasks, heap after repeated route changes. Mocked UI tests cannot measure full lifecycle behavior; browser tests were blocked by missing Playwright in this environment.
2. Bound work before normalization. Slice or reject oversized import collections before mapping; reject invalid timestamp ranges early; cap the day-splitting loop to relevant dates while keeping correct UTC day semantics.
3. Bound runtime maps. messageCounts and navigationHints lack tab-removal cleanup and enforced maxima despite declared constants. Sample repeated rate-limit warnings instead of building a stack trace and formatted log per rejected message.
4. After profiling, replace avoidable copies such as the reversed-array lookup in nodeForTab with an indexed reverse loop. A persistent node index adds invalidation complexity; measure first.
5. Keep one serialized writer. Measure full-state serialization and broadcast cost; coalesce redundant observations only if durability on worker suspension is preserved. Avoid long debounced writes.
6. Profile rendering before adding pooling, virtualization, or frameworks. Coalesce updates per frame, render visible panels only, reuse deterministic layout when topology is unchanged, and re-verify keyboard selection and reduced motion.

Acceptance criteria: correctness suite unchanged or strengthened; no unbounded retained-map growth across tab open/close cycles; no dropped distinct SPA routes; no history loss after worker restart. Set latency/heap budgets from measured baselines.

## Refactoring plan

Small, independently tested changes; keep native ES modules and Node's built-in test tools; no runtime dependencies.

1. Consolidate constants by meaning. C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\shared\constants.js declares storage keys and validation limits that differ from the active definitions in C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\shared\state.js. Enumerate consumers, delete unused definitions, and separate calendar durations from operational intervals (the DAY_MS fix is step one).
2. Extract pure calendar/statistics calculations from C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\background\service-worker.js behind an injected clock; keep browser message handling as a thin adapter.
3. Split normalization, URL policy, rewards, and persistence in C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\shared\state.js; expand dense one-line functions like compactSession into named operations without changing the schema.
4. Route import, compaction, deletion, and navigation mutations through one transaction interface with explicit no-change semantics; never mutate the shared cache without persistence (the Forget Site fix illustrates the failure mode).
5. Separate worker navigation-relationship logic from message authorization/schema checks and event registration; test allowed and denied senders behaviorally.
6. After reading all UI callers, share messaging/recovery utilities only where contracts genuinely match; do not unify unrelated page lifecycles for line count.
7. Consolidate overlapping historical audit documents into one current issue ledger with status, evidence, and verification date; keep historical reports as history.

## Product improvement suggestions

- Explain why each branch relationship was inferred and let users correct misattribution.
- Frame deep exploration neutrally ("deep branch"), never as failure; allow explicitly marking intentional deep research.
- Distinguish elapsed session time from active browsing time across idle gaps and multiple windows.
- Make retention caps visible before they bite (12 sessions, 96 nodes, 72 events, 80 compost items) and explain what is retained, with working export.
- Finish privacy deletion semantics before adding more stored metadata; make pause and exclusion controls obvious.
- Test keyboard-only use, focus restoration, contrast, zoom, reduced motion, and screen readers on real pages.
- Keep rewards optional and subordinate to reflection; avoid streak pressure.

## Tests added and validation

C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\test-service-worker.mjs gained fixed-clock calendar regressions (seven distinct UTC dates, empty streak, midnight split across two days, preserved elapsed duration, one-year-old session and node timestamps surviving import) and a stored-storage Forget Site regression that failed before the fix and passed after. Date.now is restored in a finally block; no new dependencies were added.

Observed on Node v22.20.0 (Windows):

- node test-service-worker.mjs: exit 0, after previously failing on the Forget Site regression (confirming the test exercises the bug).
- npm test (full fast suite): exit 0 — 16 state tests, service-worker, error-tracing, tree-layout, security, repository-integrity, runtime-contracts, stress all passed.
- node --check on background/service-worker.js and its test: exit 0.
- git diff --check: clean; diff covers background/service-worker.js, shared/constants.js, test-service-worker.mjs.
- npm run test:dashboard: NOT run to completion — ERR_MODULE_NOT_FOUND for playwright; install devDependencies and Chromium first.

Remaining validation: deletion under failed writes and queued navigation, terminal-node pruning behavior, worker suspension/restart durability, redirects, restricted URLs, multi-window and rapid SPA navigation in an installed extension. Origin/event deletion, descendant repair, failed-compaction cache safety, and quota/clear serialization now have passing regressions.

## Completing the requested exhaustive audit

Create a tracked file/function coverage ledger; read remaining files in small non-truncated ranges (UI sources, styles, scripts, assets, historical reports were not fully read); inspect all callers and error paths; cross-check documentation against implementation; run browser and packaging checks; then publish final findings with verified status. This interim report does not claim that exhaustive audit is complete.

## Phase 4 checkpoint: browser and packaging validation

Follow-up validation on Windows / Node v22.20.0:

- Final `npm run test:all` after the compaction fix: exit 0, including all 18 state tests, the fast contract/stress suites, and all 13 browser tests. Syntax checks passed for the changed state module and test; `git diff --check` was clean. The package was rebuilt after the fix.
- Installed the existing locked development dependencies using `npm ci --ignore-scripts --no-audit --no-fund`; no dependency declarations or lockfile changes were needed.
- `npm run test:dashboard`: exit 0, all 13 tests passed using installed Microsoft Edge. Coverage includes tree rendering, keyboard selection, narrow screens, live updates, Trusted Types rendering, SPA history, and 50 rapid SPA transitions.
- These tests run real UI sources in a browser but mock Chrome messaging/storage APIs. They do not establish installed-extension worker suspension/restart durability.
- `npm run package`: exit 0. C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\dist\focus-forest.zip contains 27 entries, a root manifest, and only the intended runtime directories/files. Archive scope was checked; installation from this archive has not been tested.
- `npm run profile:performance`: initial launch failed because bundled Chromium was missing; after installing the headless shell, retry exited 0. Observed 113 DOM elements, 0 ms layout time and about 30.05 ms style recalculation over 5.01 seconds. This is a local-file page measurement, not an installed-extension baseline; the script's empty long-task list is not sufficient evidence that no long tasks occurred.
- Badge refresh and deduplication changes in C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\background\service-worker.js and regression coverage in C:\Users\USER\Desktop\Focus-Forest-main\Focus-Forest\test-service-worker.mjs passed the fast suite in the preceding checkpoint. Those files remain staged; commit was blocked by missing Git author configuration. No push was performed.

Next: installed-extension lifecycle checks, remaining privacy/import hardening findings, deletion failure/queued-navigation coverage, and the exhaustive file/function coverage ledger. Forget Site integrity and compaction cache/concurrency regressions are green. Historical phase labels are inconsistent; this checkpoint refers to Phase 4 as testing, with Phase 5 reserved for final verification/reporting, not a claim that all audit findings are closed.
